# TURFX Frontend

React and Vite client for TURFX.

```bash
npm install
npm run dev
```

Local `/api` and `/uploads` requests are proxied to `http://localhost:5000`.
For a production build, set `VITE_API_URL` to the deployed backend API URL.
